# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "medical record"

[setup]
script = ""

[[actions]]
name = "Run"
icon = "run"
command = '''
$env:JAVA_HOME = 'C:\Program Files\Java\jdk-25.0.3'
$sdkRoot = if ($env:ANDROID_SDK_ROOT) {
    $env:ANDROID_SDK_ROOT
} else {
    'C:\Users\24493\AppData\Local\Android\Sdk'
}
$adb = Join-Path $sdkRoot 'platform-tools\adb.exe'
$emulator = Join-Path $sdkRoot 'emulator\emulator.exe'
$avd = @(& $emulator -list-avds | Where-Object { $_.Trim() } | Select-Object -First 1)
if ($avd.Count -eq 0) {
    throw 'No Android Virtual Device is installed. Create an API 26+ AVD in Android Studio first.'
}
Start-Process -FilePath $emulator -ArgumentList '-avd',$avd[0]
& $adb wait-for-device
do {
    Start-Sleep -Seconds 2
    $bootCompleted = (& $adb shell getprop sys.boot_completed).Trim()
} while ($bootCompleted -ne '1')
.\gradlew.bat installDebug
& $adb shell am start -n com.loveluke.medicalrecord.debug/com.loveluke.medicalrecord.app.MainActivity
'''
